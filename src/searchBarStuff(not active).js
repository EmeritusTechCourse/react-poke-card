/* let searchPokemon = (query) => {
  //add an alert later for if someone type in something wrong
  for(let i = 0; i < pokemon.pokeData.length; i++){
    if(query === pokemon.pokeData[i].name){
      fetch(pokemon.pokeData[i].url)
      .then(res => res.json())
      .then(pokemonInfo => pokemonInfo)
    }
  }    
}

const searchBar = () => {
  searchBar = document.getElementById('searchBar')
}


<form>
        <input type="search" id="searchBar" value=""></input>
        <button onClick={() => searchPokemon(document.getElementById('searchBar').value)}>Click me!</button>
</form> */ 